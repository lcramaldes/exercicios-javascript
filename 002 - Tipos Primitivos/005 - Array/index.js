//Array é uma lista de dados ou agrupamento de dados
//o primeiro elemento começa sempre com 0 
console.log([1, 2, 3, 4, 'teste', true]);
console.log(["larissa", "costa"]);
console.log(["larissa", "costa"].length);

['lari', 'costa'].forEach((res, index) => {
    console.log(res, index);
});

//forecah serve para usar com array e serve para
//mostrar os valore interadamente por partes conta item por item do array